import "../styles/SeconButtons.css"
const Misc = (props) => {
    return (
        <>
        <div className="SeconButtons">
            <button>{props.name}</button>
        </div>
        </>
    )
}

export default Misc;